const text = `20. When closing an XML tag, ALWAYS use a forward slash (e.g. \`</uri>\`). NEVER use a backslash (e.g. \`<\\uri>\` is STRICTLY FORBIDDEN). <\\uri> will cause a FATAL ERROR.`;
console.log("Success");
